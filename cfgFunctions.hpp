class grad_permaChoice {

	class client {
		  file = USER\grad-permaChoice\functions\client;

      class addEventhandler {preInit = 1;};
      class confirmPerma;
      class forceRespawn;
      class onPlayerKilled;
      class onPlayerRespawn;
      class onPlayerUnconscious;
      class setPlayerSpectator;
      class showNotification;
  };

	class server {
		  file = USER\grad-permaChoice\functions\server;

  };
};
